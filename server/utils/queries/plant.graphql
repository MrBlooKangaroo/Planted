query getPlant($id: ID!) {
  plant(id: $id) {
    id
    photoUrl
    nook {
        name
    }
    plantType {
        name
    }   
    waterings {
        expectedAt
    }
    createdAt
    updatedAt
  }
}