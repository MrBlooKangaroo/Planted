mutation ($input: AuthGoogleInput!) {
  authGoogle(input: $input) {
    token 
    user {
      id
      googleId
      firstName
      lastName
      email
      accessToken
      photoUrl
    }
  }
}